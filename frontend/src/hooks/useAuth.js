/**
 * @deprecated
 * This hook has been removed. Use `useAuth` from `../context/AuthContext` instead.
 *
 * This file is kept as a tombstone so any import of it gives a clear error message
 * rather than a silent "module not found".
 */
export { useAuth as default } from '../context/AuthContext';
